# Secrets — never commit your API key
.env
.env.local
*.key

# Node
node_modules/
npm-debug.log*
.npm

# Editors
.vscode/
.idea/
*.swp
.DS_Store

# Local recordings / scratch
recordings/
*.wav
!test/fixtures/*.wav
tmp/

# local backup archive
speakdown_*.tar.gz

# Invite tokens for a hosted instance (server/invite.js)
invites.json

# Hackathon form answers — contact details, live links
docs/submission.md
